pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo "pip install python"
                sh "python --v"
                sh "which python"
            }
        }
    
    }
}
