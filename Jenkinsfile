pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh "pip install python"
            }
        }
        stage("build") {
            steps{
                sh 'python myscript.py'
    }
}
    }
}
