pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo("Hello")
            }
        }
        stage("build") {
            steps{
                sh 'python mytest.py'
    }
}
    }
}
