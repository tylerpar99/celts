pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo "pip install python"
            }
        }
        stage("Build") {
            steps{
                sh 'python.exe myscript.py'
    }
}
    }
}
